def vision_agent(state):
    image = state["input"]

    # Placeholder logic
    response = "Image received. (Vision model will be added in Phase 2)"

    state["output"] = response
    return state